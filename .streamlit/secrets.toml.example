# Copy to .streamlit/secrets.toml for local runs (that file is gitignored).
# On Streamlit Community Cloud, paste the real version of this into
# Settings -> Secrets instead — never commit real keys to the repo.

# Copy to .streamlit/secrets.toml for local runs (that file is gitignored).
# On Streamlit Community Cloud, paste the real version of this into
# Settings -> Secrets instead — never commit real keys to the repo.

# Any number of passwords work — one per person you share the app with.
APP_PASSWORDS = [
    "change-me-one",
    "change-me-two",
]

GROQ_API_KEYS = [
    "gsk_key_one",
    "gsk_key_two",
]
